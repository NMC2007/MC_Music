# ============================================================
# api-gateway — Environment Variables Template
# Sao chép file này thành .env và điền giá trị thực vào
# KHÔNG commit file .env lên git
# ============================================================

# --- Server Port ---
PORT=8686

# Ghi chú: API Gateway không kết nối database
# Cấu hình URL các service được đặt trong application.yml
# theo profile: default (localhost) và docker (container name)
